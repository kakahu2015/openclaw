name: iOS Build Notify
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Build iOS IPA (Unsigned / Ad-hoc)"]
    types: [completed]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
